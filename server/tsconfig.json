{
  "compilerOptions": {
    "target": "ES2020",
    "module": "commonjs",

    // Keep all source files inside src
    "rootDir": "src",
    "outDir": "dist",

    // Compatibility and type safety
    "esModuleInterop": true,
    "skipLibCheck": true,
    "strict": false,

    // Ensure Node types are available
    "types": ["node"]
  },
  "include": ["src/**/*", "cookie-parser"],
  "exclude": [
    "node_modules",
    "generated", // old duplicate prisma folder
    "prisma.config.ts" // config file outside src
  ]
}
